# CPU-specific dependencies for MLX Video Backend
# Uses CPU-only PyTorch build

# Core dependencies
grpcio>=1.60.0
grpcio-tools>=1.60.0
requests>=2.31.0
numpy>=1.24.0
Pillow>=10.0.0
tqdm>=4.66.0

# CPU-only PyTorch (no CUDA/MPS)
torch>=2.1.0 --index-url https://download.pytorch.org/whl/cpu

# Video generation libraries
diffusers>=0.24.0
transformers>=4.35.0
accelerate>=0.24.0

# Video processing
imageio>=2.33.0
imageio-ffmpeg>=0.4.9
